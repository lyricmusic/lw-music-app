{
  "rules": {
    ".read": false,
    ".write": false,
    "roomPresence": {
      "$roomId": {
        ".read": "auth != null",
        "$uid": {
          "$connectionId": {
            ".write": "auth != null && auth.uid === $uid",
            ".validate": "newData.isNumber() && newData.val() <= now"
          }
        }
      }
    }
  }
}
